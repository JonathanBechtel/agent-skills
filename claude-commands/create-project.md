---
description: Convert a spec document into a GitHub Project with orchestrator-ready tickets
argument-hint: <spec-file> [--owner OWNER] [--repo REPO] [--project-name NAME] [--model MODEL] [--qa-checklist QA_FILE] [--test-plan TEST_PLAN_FILE] [--conda-env ENV]
---

Run the `create-project` skill end-to-end using `$ARGUMENTS`.

The full procedure lives in the skill (`~/.claude/skills/create-project/SKILL.md`), which is the single source shared with Codex CLI. Read it and follow it.

**Usage:** `/create-project <spec-file-path> [--owner OWNER] [--repo REPO] [--project-name NAME] [--model MODEL] [--qa-checklist QA_FILE] [--test-plan TEST_PLAN_FILE] [--conda-env ENV]`
